1. O nome Linux surgiu da mistura de Linus + Unix. Linus é o nome do criador do Linux, Linus Torvalds.
   E Unix, é o nome de um sistema operacional de grande porte.

2. Um daemon é um programa de computador que executa como um processo em plano de fundo, em vez de estar 
   sob o controle direto de um usuário interativo.
   
3. Shell é um programa que intermedeia o contato entre o usuário e o computador. É a interface entre o usuário 
   e o sistema operacional (linha de comando).
   
4. A execussão do terminal como super-usuário permite ao usuário livre acesso ao sistema, podendo executar comandos 
   e realizar edições que podem acarretar em uma desconfiguração desse mesmo sistema.
   
5. Tab.

6. Seta para cima.

7. a) man 
   b) ls
   c) pwd
   d) cd
   e) mkdir
   f) rm
   g)
   h) cp
   i)
   j) mv
   k)
   l) rename
   m)
   n)
   o) clear
   p)
   q)
   r)
   s)
   t)
